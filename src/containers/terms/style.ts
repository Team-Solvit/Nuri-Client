import styled from '@emotion/styled';

export const Page = styled.main`
  margin: 40px auto;
  padding: 0 20px;
  line-height: 1.7;
  max-height: 90vh;
  overflow-y: auto;
`;

export const Title = styled.h1`
  font-size: 28px;
  font-weight: 700;
  margin-bottom: 16px;
`;

export const Effective = styled.p`
  color: #666;
  margin-bottom: 24px;
`;

export const Section = styled.section`
  margin-bottom: 24px;
`;

export const SectionTitle = styled.h2`
  font-size: 18px;
  font-weight: 700;
  margin: 16px 0 8px;
`;

export const List = styled.ul`
  padding-left: 18px;
`;
