export interface UserProfile {
  userId: string;
  name?: string;
  profile?: string;
  introduce?: string;
}

export interface UserSearchResult {
  userId: string;
  name?: string;
  profile?: string;
}
